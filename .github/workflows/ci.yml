name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    # Removed services block to rule out resource constraints
    env:
      # Must match the provider in schema.prisma (postgresql), even if service is off
      DATABASE_URL: "postgresql://postgres:password@localhost:5432/test_db"
      NEXTAUTH_SECRET: supersecret
      NEXTAUTH_URL: http://localhost:3000

    steps:
    - uses: actions/checkout@v4

    - name: Setup Bun
      uses: oven-sh/setup-bun@v1
      with:
        bun-version: latest

    - name: Install dependencies
      run: bun install

    - name: Generate Prisma Client
      run: bun x prisma generate

    - name: Run Linter
      run: bun run lint

    - name: Type Check
      run: bun x tsc --noEmit

    - name: Run Tests
      run: bun run test

    - name: Build
      run: bun run build
